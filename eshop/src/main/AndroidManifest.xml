<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools"
          package="com.edu.nbl.eshop">

    <!-- 网络权限 -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name=".EasyShopApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Design.Light.NoActionBar"
        tools:replace="android:icon,android:name">
        <activity android:name=".Activity.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".Activity.MainActivity"/>
        <activity android:name=".User.login.LoginActivity">
        </activity>
        <activity android:name=".User.register.RegisterActivity">
        </activity>
        <activity android:name=".Activity.me.personinfo.PersonActivity">
        </activity>
        <activity android:name=".Activity.me.personinfo.NickNameActivity"/>
        <activity android:name=".Activity.me.persongoods.PersonGoodsActivity">
        </activity>
        <activity android:name=".Activity.me.goodsupdata.GoodsUploadActivity">
        </activity>
    </application>

</manifest>